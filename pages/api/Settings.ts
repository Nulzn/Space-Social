import { NextApiRequest, NextApiResponse } from "next";

export default function settingsHandler(req: NextApiRequest, res: NextApiResponse) {
    // Send the settings information, with the token to the database handler

    
}